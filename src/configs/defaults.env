BASE_MODEL="${BASE_MODEL:-OpenGVLab/InternVL3-2B}"
NUM_FRAMES="${NUM_FRAMES:-16}"
H5_SHARDS="${H5_SHARDS:-16}"
MICROLENS_MLLM_CHECKPOINT="${MICROLENS_MLLM_CHECKPOINT:-checkpoint-2961}"
TOPICVID_MLLM_CHECKPOINT="${TOPICVID_MLLM_CHECKPOINT:-checkpoint-5298}"

K="${K:-20}"
EPOCHS="${EPOCHS:-200}"
BATCH_SIZE="${BATCH_SIZE:-256}"
LEARNING_RATE="${LEARNING_RATE:-0.0001}"
HEADS="${HEADS:-16}"
HIDDEN="${HIDDEN:-512}"
PATIENCE="${PATIENCE:-20}"
WEIGHT_DECAY="${WEIGHT_DECAY:-0.0001}"
SEED="${SEED:-42}"
